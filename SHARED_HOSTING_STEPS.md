# 📋 خطوات الرفع على Shared Hosting (مبسطة)

## 🎯 **الطريقة السريعة:**

### 1️⃣ **تحضير الملفات** (5 دقائق)
```bash
# في مجلد المشروع
zip -r learning-platform.zip . -x "*.git*" "node_modules/*" "tests/*" "*.md"
```

### 2️⃣ **في cPanel** (10 دقائق)

#### أ) إنشاء قاعدة البيانات:
- **MySQL Databases** → إنشاء قاعدة بيانات
- احفظ البيانات: `اسم_المستخدم_platform`

#### ب) رفع الملفات:
1. **File Manager** → `public_html/`
2. رفع `learning-platform.zip`
3. **Extract** الملف
4. نقل محتويات `public/` إلى `public_html/`
5. تحديث `index.php` في `public_html/`

### 3️⃣ **الإعداد النهائي** (5 دقائق)

#### أ) تحديث .env:
```env
DB_DATABASE=اسم_قاعدة_البيانات
DB_USERNAME=اسم_المستخدم
DB_PASSWORD=كلمة_المرور
APP_URL=https://yourdomain.com
APP_DEBUG=false
```

#### ب) تشغيل الإعداد:
- اذهب إلى: `https://yourdomain.com/setup.php`
- اتبع التعليمات
- احذف `setup.php` بعد الانتهاء

### 4️⃣ **اختبار النظام** (2 دقيقة)
- سجل دخول: `admin@platform.com` / `123456789`
- **غير كلمة المرور فوراً!**
- اختبر إنشاء كورس وامتحان

---

## 🔧 **إذا واجهت مشاكل:**

### خطأ 500:
```bash
# في Terminal cPanel
cd public_html
chmod -R 775 learning-platform/storage
chmod -R 775 learning-platform/bootstrap/cache
```

### قاعدة البيانات لا تعمل:
- تأكد من بيانات قاعدة البيانات في .env
- تأكد من إضافة المستخدم لقاعدة البيانات

### الصور لا تظهر:
```bash
php artisan storage:link
```

---

## 🎉 **بعد الرفع الناجح:**

### ✅ **يمكنك:**
1. **إدارة الطلاب والكورسات**
2. **إنشاء امتحانات وأسئلة**
3. **مراقبة الأنشطة**
4. **إرسال الرسائل** (بعد ربط WhatsApp API)
5. **توليد التقارير الشهرية**

### 🔒 **للأمان:**
- غير كلمة مرور المدير
- احذف ملف setup.php
- فعل SSL Certificate
- عمل نسخة احتياطية دورية

---

## 💡 **نصائح Shared Hosting:**

### للأداء الأفضل:
```env
# في .env
CACHE_STORE=database
SESSION_DRIVER=database
QUEUE_CONNECTION=database
LOG_LEVEL=error
APP_DEBUG=false
```

### للأمان:
- استخدم كلمات مرور قوية
- فعل SSL
- حدث Laravel دورياً
- راقب سجل الأنشطة

**🎯 المدة الإجمالية: 20-30 دقيقة للرفع الكامل!**
